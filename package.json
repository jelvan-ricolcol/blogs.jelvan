{
  "name": "blogs-jelvan-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint './src/**/*.ts'",
    "deploy": "wrangler publish --config wrangler.toml --env production"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
